# Claude Code

Project instructions live in @AGENTS.md.

Skills are mirrored from `.opencode/skill/` into `.claude/skills/` by
`scripts/sync_agent_config.py` (CI enforces they stay in sync). The graphify
knowledge graph can be rebuilt with `graphify update .`.
