# Claude Code

Project instructions live in @AGENTS.md.

Agent definitions are canonical under `agent/` (skills, MCP servers, hooks).
Per-tool files including `.claude/skills/` and `.claude/settings.json` are
generated by `scripts/sync_agent_config.py`; CI enforces they stay in sync.
