{
  "description": "Generated by scripts/sync_agent_config.py from agent/ — edit the source, not this file.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"$(git rev-parse --show-toplevel)/scripts/graphify_reminder.py\" --format codex",
            "statusMessage": "Loading knowledge graph hint",
            "additionalContextLimit": 1000
          }
        ]
      }
    ]
  }
}
